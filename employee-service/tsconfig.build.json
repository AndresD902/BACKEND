{
  // Dedicated config for production builds — excludes test files and
  // emits output to dist/. Used by the "build" npm script.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src"]
}
